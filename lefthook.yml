pre-commit:
  parallel: true
  commands:
    lint:
      run: make lint
    format-check:
      run: make format
    test:
      run: make test
    archgate:
      run: command -v archgate >/dev/null 2>&1 && archgate check --staged || true
      skip:
        - merge
        - rebase
