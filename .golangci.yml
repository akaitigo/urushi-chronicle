version: "2"

run:
  go: "1.24"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

formatters:
  enable:
    - gofumpt

issues:
  exclude-dirs:
    - vendor
